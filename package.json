{
  "name": "visual-essays",
  "version": "0.3.0",
  "description": "Visual Essays",
  "author": "Ron Snyder",
  "private": false,
  "scripts": {
    "netlify-build": "cd client && npm i && npm run build; cd ../client-lib && npm i && npm run netlify-build;"
  },
  "dependencies": {
    "serverless": "^1.47.0",
    "serverless-google-cloudfunctions": "^2.3.3"
  }
}
